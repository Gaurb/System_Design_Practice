package TicTacToePractice.user.strategy;

import TicTacToePractice.game.Board;
import TicTacToePractice.game.Cell;

public class MinMaxBotPlayingStrategy implements BotPlayingStrategy{
    @Override
    public Cell getNextMove(Board board) {
        return null;
    }
}
